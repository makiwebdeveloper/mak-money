# Этап 4 — Транзакции и Распределения (Allocations)

## ✅ Реализовано

### Базовая архитектура

**Ключевое понимание:**
- **Счета (Accounts)** — реальные места хранения денег (банковские счета, карты, кошельки)
- **Транзакции (Transactions)** — реальное движение денег (доходы, расходы, переводы)
- **Пулы (Money Pools)** — виртуальное распределение денег по целям
- **Распределения (Allocations)** — связь между счетами и пулами (показывает, сколько денег из конкретного счета выделено в конкретный пул)

### 1. База данных

#### Миграции:

**`004_create_transactions.sql`**
- Таблица `transactions` с типами: `income`, `expense`, `transfer`
- Поля: amount, currency, account_id, from_account_id, to_account_id, category, description, transaction_date
- Автоматическое обновление балансов счетов через триггеры
- Валидация типов транзакций (income/expense требуют account_id, transfer требует from/to)
- RLS политики для безопасности данных

**`005_create_allocations.sql`**
- Таблица `allocations` для связи счетов и пулов
- Уникальное ограничение: один allocation на пару account-pool
- Валидация: сумма allocations не может превышать баланс счета
- Функции для расчета балансов пулов и нераспределенных средств

**`006_auto_allocate_income.sql`**
- Автоматическое распределение доходов в пул "Свободные"
- Триггеры для создания/обновления allocations при добавлении дохода
- Корректное удаление allocations при удалении транзакции

### 2. TypeScript типы

Обновлен [src/lib/types/database.ts](src/lib/types/database.ts):
- `TransactionType`: "income" | "expense" | "transfer"
- Типы для таблиц `transactions` и `allocations`
- Row, Insert, Update интерфейсы для type-safety

### 3. API Эндпоинты

#### Transactions API

**`GET /api/transactions`**
- Получение всех транзакций пользователя
- Фильтры: `type`, `startDate`, `endDate`
- Сортировка по дате (новые первые)

**`POST /api/transactions`**
- Создание новой транзакции
- Валидация типов и обязательных полей
- Автоматическое обновление баланса счета (через триггер БД)
- Автоматическое распределение дохода в пул "Свободные"

**`GET /api/transactions/[id]`**
- Получение отдельной транзакции

**`DELETE /api/transactions/[id]`**
- Удаление транзакции
- Автоматический откат изменений баланса

#### Allocations API

**`GET /api/allocations`**
- Получение всех распределений
- Фильтры: `account_id`, `pool_id`
- Включает связанные данные счетов и пулов

**`POST /api/allocations`**
- Создание/обновление распределения (upsert)
- Валидация: allocations не превышают баланс счета
- Проверка принадлежности счета и пула пользователю

**`PUT /api/allocations/[id]`**
- Обновление суммы распределения

**`DELETE /api/allocations/[id]`**
- Удаление распределения

### 4. UI Компоненты

**Страница транзакций**: [src/app/transactions/page.tsx](src/app/transactions/page.tsx)
- Список всех транзакций пользователя
- Отображение типа, суммы, даты, категории
- Цветовая кодировка: доходы (зеленый), расходы (красный), переводы (синий)
- Server-side рендеринг для оптимальной производительности

---

## 📊 Как это работает

### Пример потока данных:

1. **Пользователь получает зарплату 50,000₽**
   - Создается транзакция: `type: "income", amount: 50000, account_id: "Сбербанк"`
   - Триггер БД обновляет баланс счета "Сбербанк": `+50,000₽`
   - Триггер БД создает allocation: `account: "Сбербанк", pool: "Свободные", amount: 50000`

2. **Пользователь перераспределяет деньги по пулам**
   - Через API allocations пользователь может распределить:
     - 30,000₽ остаются в "Свободные"
     - 15,000₽ в пул "Подписки"
     - 5,000₽ в пул "Накопления"
   - Счет "Сбербанк" по-прежнему имеет 50,000₽
   - Но теперь эти деньги виртуально распределены по трем пулам

3. **Пользователь оплачивает подписку 500₽**
   - Создается транзакция: `type: "expense", amount: 500, account_id: "Сбербанк"`
   - Баланс счета: 50,000 - 500 = 49,500₽
   - Пользователь вручную уменьшает allocation в пуле "Подписки" на 500₽

### Важные правила:

✅ **Сумма всех allocations для счета ≤ баланса счета**
✅ **Баланс пула = сумма всех allocations в этот пул**
✅ **Транзакции всегда работают со счетами, не с пулами**
✅ **Пулы — это просто метки для визуализации**

---

## 🚀 Следующие шаги

Этап 4 завершен. Основной функционал:
- ✅ Создание доходов, расходов, переводов
- ✅ Автоматическое обновление балансов
- ✅ Автоматическое распределение доходов
- ✅ Ручное управление распределениями
- ✅ История транзакций

**Для полного MVP осталось:**
- Этап 5: Dashboard (главный экран с балансами и быстрыми действиями)
- Этап 6: История и отчеты (фильтры, графики)
- UI формы для создания транзакций (сейчас только API готово)

---

## 📝 Технические детали

### Database Triggers:
1. `update_account_balance_on_transaction` — обновляет баланс при создании транзакции
2. `revert_account_balance_on_transaction_delete` — откатывает баланс при удалении
3. `allocate_income_to_free_pool` — автоматически распределяет доходы
4. `deallocate_income_from_free_pool` — корректирует при удалении дохода
5. `validate_allocation_amount` — проверяет, что allocations не превышают баланс

### Security:
- Row Level Security (RLS) на всех таблицах
- Пользователи видят только свои данные
- Валидация на уровне БД и API

---

**Реализация полностью соответствует canvas-документу и принципу: "Пулы — это виртуальное отображение, транзакции — реальное движение денег".**
